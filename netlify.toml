# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA (React Router): qualquer rota serve o index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
